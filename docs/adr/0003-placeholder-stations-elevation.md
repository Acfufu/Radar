# 占位站转正:分站 = 同一数据面 + 模型白名单,与官网同构

spec §4.1「占位站永不同步」的前提(上游非 Codex 站无数据)已失效:2026-09-08 官网聚合/DSH/ZCode/Grok 四预览站全部带真实数据(聚合站 4 站 40 可比档位、近 24h 运行 127),机制为同一批端点 + 前端模型白名单裁剪,无独立分站端点,`?station=` 仅前端参数。拍板:四占位站一轮全部转正为实站;数据复用既有 intelligence-efficiency sidecar(其数据集已含全部新站模型档位,开放集解析,零新端点);分站归组按模型白名单(与官网同构),不依赖上游 harness 字段(实测标注不一致:dsh 已标,glm/grok 误标 codex)。聚合站的跨站对比保留「独立来源指标不构成统一排名」的持久声明并逐点标注来源站。

## Consequences

- §4.1 占位站契约按站逐个退役;Kimi 站维持占位卡,等上游开放。
- 聚合站视图是「同基准并表 + 来源标注」,不引入跨站统一排名或复合评分(产品红线不变)。
