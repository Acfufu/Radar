# AI Radar

macOS 原生 App,只读聚合 codexradar.com 上游公开数据面的 AI 编码模型效能雷达;上游按「站」划分板块,App 侧一一镜像为独立工作区。

## Language

**站 (Station)**:
上游按 harness/产品线划分的数据板块(Claude Radar / Codex / SWE-bench / DSH / ZCode / Grok);App 侧一个站对应一个独立工作区、数据源与路由。
_Avoid_: 频道、标签页

**实站**:
有真实数据同步的站(独立 runtime + 数据面 + 页面)。
_Avoid_: 正式站、上线站

**预览站(占位态)**:
上游已渲染真实数据、App 侧尚未接入的站,侧栏以占位卡呈现、不同步;转正后即成为实站,「占位」只描述站的接入阶段,不是站的种类。
_Avoid_: 假站、空站

**聚合站**:
把多个站的同基准档位并表对比的站;每个档位点必须标注来源站,且不构成跨站统一排名。

**同域公开数据面**:
codexradar.com 同域下无需凭据的公开只读 GET 端点全体,既含 `/data/*` 静态文件,也含 `/api/*` 只读查询(见 ADR-0001)。
_Avoid_: 公开 API(过宽,易混入禁域的提交类端点)

**模型白名单**:
站→模型 ID 集合的映射,用于从共享数据面裁剪出单站视图;分站归组以它为准,与官网前端同构。
_Avoid_: harness 字段归组(上游标注不一致,不作为分站依据)

**渲染读取器 (Rendered Reader)**:
经非持久 WebKit 观察上游渲染页 DOM 的只读读取路径,是数据面缺失时的最后手段;数据面可用时应优先数据面(见 ADR-0002)。
_Avoid_: 爬虫、抓取

**Sidecar**:
挂在主同步链之外的独立数据面同步器(专属 Coordinator + transport + dataset type),沿用渲染读取器验证过的接线模式。
_Avoid_: 插件、子任务
